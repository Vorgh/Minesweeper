/**
 * The custom classes for the GUI, which are not purely in FXML. The package contains only these classes, and the
 * {@code MainView}, which initializes the FXML files and sets up the whole GUI.
 */
package minesweeper.view;